Sí. Ya vi el bug principal, y no está en frmLector.cs sino en la combinación de FormAlerta + UtilidadesSVG.
En tu repo actual:
FormAlerta.CargarIcono(...) primero llama utilSVG.ObtenerRutaSVG(svgFileName), y luego le pasa ese resultado a utilSVG.CargarIconoSVG(...). �
GitHub +1
Pero CargarIconoSVG(...) vuelve a llamar internamente a ObtenerRutaSVG(svgFileName). �
GitHub
O sea, hoy estás haciendo esto dos veces:
resolver nombre → ruta
volver a tratar esa ruta como si fuera nombre
Y además, cuando svgPath queda vacío, CargarIconoSVG(...) muestra la alerta:
C#
MessageBox.Show($"⚠️ Archivo SVG no encontrado: {svgPath}", ...)
por eso te sale el warning con ruta vacía. �
GitHub
Por qué igual ves el icono correcto
Porque en tus diseñadores los PictureBox ya tienen una imagen por defecto cargada desde Properties.Resources, por ejemplo en FormAlerta, FormAlertaError y FormAlertaOk. Entonces aunque falle la carga SVG, el recurso del diseñador sigue visible. �
GitHub +2
Qué pasa en frmLector.cs
frmLector ya está llamando:
MostrarAlerta(msg) para error, que abre FormAlertaError
AlertaManager.MostrarAlerta("Registrado Ok", ...) para éxito
y también tiene helper AlertaOk(...) para FormAlertaOk aunque no veo que lo use en el flujo exitoso actual. �
GitHub +1
Además, en el repo actual frmLector_Load ya sí crea m_pCoreScanner, obtiene discoverScanner, registra eventos y deja el form listo para conectar; ese problema ya no es el actual. �
GitHub
Fix real
1) Corrige FormAlerta.cs
Tu bug está aquí:
C#
string svgPath = utilSVG.ObtenerRutaSVG(svgFileName);
utilSVG.CargarIconoSVG(tipo == 1 ? icon_ok : (tipo == 2 ? icon_info : icon_error), svgPath);
Debe quedar así:
C#
private void CargarIcono(int tipo)
{
    string svgFileName;

    switch (tipo)
    {
        case 1:
            icon_ok.Visible = true;
            svgFileName = "icono_check_512.svg";
            break;
        case 2:
            icon_info.Visible = true;
            svgFileName = "icono_info_512.svg";
            break;
        default:
            icon_error.Visible = true;
            svgFileName = "icono_error_512.svg";
            break;
    }

    PictureBox target = tipo == 1 ? icon_ok : (tipo == 2 ? icon_info : icon_error);
    utilSVG.CargarIconoSVG(target, svgFileName);
}
No llames ObtenerRutaSVG(...) antes. Deja que CargarIconoSVG(...) lo haga una sola vez.
2) Corrige FormAlertaOk.cs
Ahí también estás haciendo doble resolución:
FormAlertaOk_Load llama ObtenerRutaSVG("icono_checking_512.svg")
y luego le pasa esa ruta a CargarIconoSVG(...), que vuelve a resolver. �
GitHub +1
Cámbialo por:
C#
private void FormAlertaOk_Load(object sender, EventArgs e)
{
    Tiempo.Start();

    var utilSVG = new UtilidadesSVG();
    utilSVG.CargarIconoSVG(this.icon_ok, "icono_checking_512.svg");
}
3) Mejora UtilidadesSVG.cs
Ahora mismo si no encuentra el archivo, el mensaje muestra svgPath, que puede quedar vacío. �
Mejor así:
GitHub
C#
public void CargarIconoSVG(PictureBox pictureBox, string svgFileName)
{
    if (pictureBox == null)
    {
        MessageBox.Show("❌ El PictureBox es NULL", "Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
        return;
    }

    if (string.IsNullOrWhiteSpace(svgFileName))
    {
        MessageBox.Show("⚠️ El nombre del archivo SVG está vacío.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Warning);
        return;
    }

    string svgPath = ObtenerRutaSVG(svgFileName);

    if (string.IsNullOrEmpty(svgPath))
    {
        MessageBox.Show($"⚠️ Archivo SVG no encontrado: {svgFileName}", "Error", MessageBoxButtons.OK, MessageBoxIcon.Warning);
        return;
    }

    try
    {
        SvgDocument svgDocument = SvgDocument.Open(svgPath);
        if (svgDocument == null)
        {
            MessageBox.Show("⚠️ No se pudo cargar el SVG.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Warning);
            return;
        }

        Bitmap bitmap = svgDocument.Draw();
        if (bitmap == null)
        {
            MessageBox.Show("⚠️ Error al convertir SVG a Bitmap.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Warning);
            return;
        }

        pictureBox.Image = bitmap;
        pictureBox.SizeMode = PictureBoxSizeMode.Zoom;
    }
    catch (Exception ex)
    {
        MessageBox.Show("❌ Error cargando SVG: " + ex.Message, "Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
    }
}
Sobre la ruta Ini
Sí, puedes reutilizar DBConsultas.LoadJsonConfig("tsconfig.json") en Sybase.cs y MySQLconexion.cs; eso está bien. Pero para SVG no mezcles esa lógica. Tus SVG no vienen de tsconfig.json; solo necesitas resolver la carpeta correcta.
Ahora mismo UtilidadesSVG.ObtenerRutaSVG(...) busca en AppDomain.CurrentDomain.BaseDirectory + "Ini", o sea bin\x64\Debug\Ini. �
Si tus SVG están en otra carpeta, tendrás que copiarlos ahí o cambiar esa ruta.
GitHub
Qué revisar ya mismo
Haz estas dos búsquedas en tu repo y corrige todas:
ObtenerRutaSVG(
CargarIconoSVG(
La regla es: si llamas CargarIconoSVG, pásale el nombre del archivo, no la ruta ya resuelta.
En frmLector.cs
No veo un bug nuevo ahí por el tema SVG. El flujo actual de alertas usa:
FormAlertaError para MostrarAlerta(msg)
AlertaManager.MostrarAlerta("Registrado Ok", ...) para éxito. �
GitHub
Así que el warning del SVG viene de los formularios de alerta, no del lector.
Pégame tu UtilidadesSVG.cs, FormAlerta.cs y FormAlertaOk.cs actuales si quieres que te los devuelva ya corregidos línea por línea.