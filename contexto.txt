Vamos con UI, pero primero deja compilando.
Tus errores son por dos cosas:
1. Falta crear VinculacionRequest.cs
Crea un archivo nuevo llamado:
Plain text
VinculacionRequest.cs
y pon esto:
C#
namespace DS9908R_App
{
    public class VinculacionRequest
    {
        public string CodigoBarras { get; set; }
        public string Rfid { get; set; }
        public string HojaMarcacion { get; set; }
        public string CodTrabajador { get; set; }
        public string Empresa { get; set; }
    }
}
2. Falta el using de ConcurrentQueue
En VinculadorService.cs, arriba, agrega:
C#
using System.Collections.Concurrent;
y deja los usings así:
C#
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Data;
using System.Threading.Tasks;
3. Si tu proyecto es viejo, verifica que el archivo esté incluido en el .csproj
Si Visual Studio no lo agrega solo, mete esto dentro del <ItemGroup> de los Compile:
XML
<Compile Include="VinculacionRequest.cs" />
<Compile Include="VinculadorService.cs" />
4. Si ConcurrentQueue sigue fallando
Eso sería raro en .NET 4.8, pero si pasa, agrega también esta referencia en el .csproj dentro de <ItemGroup> de referencias:
XML
<Reference Include="System.Core" />
Quedaría así:
XML
<ItemGroup>
  <Reference Include="System" />
  <Reference Include="System.Core" />
  <Reference Include="System.Data" />
  <Reference Include="System.Deployment" />
  <Reference Include="System.Drawing" />
  <Reference Include="System.Windows.Forms" />
  <Reference Include="System.Xml" />
  <Reference Include="System.Xml.Linq" />
</ItemGroup>
Resumen exacto
Haz estas 3 cosas:
crear VinculacionRequest.cs
agregar using System.Collections.Concurrent;
verificar que ambos .cs estén incluidos en el .csproj
Después de eso vuelve a compilar.
Cuando pase ese error, seguimos con la UI de tabVinculador y el grid dgvTagList.