Sí, puedes quitar ToolStripStatusLabel y usar MsnVincular como estado principal. De hecho, con tu diseño nuevo tiene más sentido: ya moviste la conexión a un panel y el tab arriba, así que una barra inferior tipo status strip ya no es obligatoria. Revisé el repo, pero formLector.Designer.txt y frmLector.cs están prácticamente en una sola línea en el raw, así que no se pueden inspeccionar bien por bloques; aun así, por lo que describes, la mejor salida es no recrear ToolStripStatusLabel y centralizar el estado en un solo control visible. �
GitHub +1
Te recomiendo esto:
Opción recomendada
Usa MsnVincular para:
mensajes de conexión
errores de lectura
estado de guardado
limpieza automática
RFID repetido o múltiples RFID
Y deja el panel de conexión solo para:
btnBuscarScanners
cmbScanners
un label visual de estado, por ejemplo lblEstadoConexion
Así separas:
estado técnico de conexión → lblEstadoConexion
mensajes operativos del usuario → MsnVincular
Qué cambiar en frmLector.cs
1) Reemplaza toolStripStatusLbl.Text = ...
Por esto:
C#
private void SetMensajeVincular(string mensaje)
{
    if (MsnVincular == null)
        return;

    if (MsnVincular.InvokeRequired)
    {
        MsnVincular.Invoke(new Action<string>(SetMensajeVincular), mensaje);
        return;
    }

    MsnVincular.Text = mensaje ?? "";
}
2) Crea un helper de estado visual
Así ya no dependes del ToolStripStatusLabel:
C#
private void SetEstadoGeneral(string mensaje, Color color)
{
    SetMensajeVincular(mensaje);

    if (MsnVincular != null)
    {
        MsnVincular.ForeColor = color;
    }
}
3) Cambia SetEstado(...)
Si hoy tu método usa toolStripStatusLbl, reemplázalo por:
C#
private void SetEstado(string mensaje, Color color)
{
    SetEstadoGeneral(mensaje, color);
}
Dónde se rompe hoy
Seguramente tienes varias líneas así:
C#
toolStripStatusLbl.Text = "..."
toolStripStatusLbl.BackColor = ...
Debes cambiarlas por:
C#
SetEstado("...", Color.Khaki);
o si solo quieres texto:
C#
SetMensajeVincular("...");
Ejemplos concretos
En Vinculador_OnError
C#
private void Vinculador_OnError(string msg)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<string>(Vinculador_OnError), msg);
        return;
    }

    SetEstado(msg, Color.Firebrick);
    ReproducirError();

    AlertaManager.MostrarAlerta(msg, pinturaRoja, 3, 5);

    CodBarras.Clear();
    CodBarras.Focus();
}
En Vinculador_OnInfo
C#
private void Vinculador_OnInfo(string msg)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<string>(Vinculador_OnInfo), msg);
        return;
    }

    SetEstado(msg, Color.DarkOrange);
}
En Vinculador_OnInsertadoOk
C#
private void Vinculador_OnInsertadoOk(Dictionary<string, object> data)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<Dictionary<string, object>>(Vinculador_OnInsertadoOk), data);
        return;
    }

    if (data != null)
    {
        if (data.ContainsKey("op"))
            TextBoxOP.Text = Convert.ToString(data["op"]);

        if (data.ContainsKey("hoja_marcacion"))
            TextBoxHM.Text = Convert.ToString(data["hoja_marcacion"]);
    }

    SetEstado("Prenda registrada exitosamente.", Color.Green);
    LlenarPrimeraFilaDataGridView1(data);
    ActualizarTotalCount();

    ReproducirOk();
    AlertaManager.MostrarAlerta("Registrado Ok", pinturaVerde, 1, 5);

    CodBarras.Clear();
    _ultimoCodigoBarra = "";
    _ultimoRfid = "";
    _rfidPendientes.Clear();
    dgvTagList.Rows.Clear();
    cantidadRFID.Text = "0";
    CodBarras.Focus();
}
Para la conexión del scanner
Mantén lblEstadoConexion en el panel superior, por ejemplo:
C#
private void ActualizarEstadoConexion(string texto, Color color)
{
    lblEstadoConexion.Text = texto;
    lblEstadoConexion.BackColor = color;
    lblEstadoConexion.ForeColor = Color.White;
    lblEstadoConexion.TextAlign = ContentAlignment.MiddleCenter;

    SetMensajeVincular(texto);
}
Así no pierdes feedback.
Mi recomendación final
No vuelvas a crear ToolStripStatusLabel.
Con tu diseño nuevo, queda mejor así:
arriba izquierda: panel de conexión con btnBuscarScanners, cmbScanners, lblEstadoConexion
arriba o debajo del panel: MsnVincular como mensaje principal del sistema
tabs arriba
grillas abajo
Eso te deja una UI más limpia y más parecida a un sistema productivo moderno.
Si quieres, te doy el bloque exacto para buscar y reemplazar todas las referencias a toolStripStatusLbl dentro de frmLector.cs.