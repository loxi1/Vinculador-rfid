Perfecto, ahora sí ya tengo todo el contexto completo (UI + C# + SP Sybase). Te hago una validación realista de producción, no genérica.
🔍 1. VALIDACIÓN GENERAL (CRÍTICO)
Tus formularios están bien migrados visualmente, pero hay 3 problemas típicos que explican lo que te pasa:
👉 “leyó pero no guardó y no muestra bien alerta”
Problemas detectados:
❗ No estás parseando correctamente el mensaje del SP
❗ No estás usando el tipo de alerta según codigo
❗ Tus formularios están bien, pero no estás llamándolos correctamente
🧠 2. CLAVE: RESPUESTA DEL SP (ESTO ES EL CORE)
Tu SP devuelve SIEMPRE:
SQL
SELECT @flg_error AS codigo, @vv_mensaje AS mensaje
Ejemplo real:
codigo
mensaje
0
OK
1
La Prenda No Fue Ingresada...
3
Error: El RFID ya existe
⚠️ ERROR PRINCIPAL EN TU APP
Estoy casi seguro que estás haciendo algo así:
C#
if (resultado == "OK")
❌ MAL
✅ FORMA CORRECTA (OBLIGATORIO)
En VinculadorService.cs:
C#
using (AseCommand cmd = new AseCommand("USP_SAL_EMB_CON_RFID", conn))
{
    cmd.CommandType = CommandType.StoredProcedure;

    cmd.Parameters.AddWithValue("@etqt", etqt);
    cmd.Parameters.AddWithValue("@empresa", empresa);
    cmd.Parameters.AddWithValue("@usr", usr);
    cmd.Parameters.AddWithValue("@rfid", rfid);
    cmd.Parameters.AddWithValue("@nHoja", nHoja);

    using (AseDataReader reader = cmd.ExecuteReader())
    {
        if (reader.Read())
        {
            int codigo = Convert.ToInt32(reader["codigo"]);
            string mensaje = reader["mensaje"].ToString();

            if (codigo == 0)
            {
                OnInsertadoOk(...);
            }
            else
            {
                OnError(mensaje);
            }
        }
    }
}
🎯 3. VALIDACIÓN DE TUS FORMULARIOS (UNO POR UNO)
✅ FormAlerta.cs (toast tipo notificación)
✔ Correcto para:
mensajes rápidos
no bloqueantes
👉 USO CORRECTO:
C#
AlertaManager.MostrarAlerta("Registrado OK", pinturaVerdeMedio, 1, 5);
✅ FormAlertaError.cs (modal fuerte)
✔ Correcto para:
errores del SP
validaciones
👉 USO CORRECTO:
C#
MostrarAlerta(msg);
⚠️ FormAlertaOk.cs (ojo aquí)
Este lo tienes bien visualmente, PERO:
Problema:
Usa Timer
pero si no haces Tiempo.Start() → nunca cierra
✅ SOLUCIÓN
En FormAlertaOk_Load:
C#
private void FormAlertaOk_Load(object sender, EventArgs e)
{
    Tiempo.Interval = tiempo * 1000; // segundos
    Tiempo.Start();
}
Y:
C#
private void Tiempo_Tick(object sender, EventArgs e)
{
    Tiempo.Stop();
    this.Close();
}
⚠️ 4. ERROR REAL QUE ESTÁS TENIENDO
Tu SP tiene MUCHOS errores de negocio tipo:
SQL
SET @vv_mensaje = 'La Prenda No Fue Ingresada Acabados...'
SET @flg_error = 1
👉 Pero en C# probablemente estás:
ignorando el código
o no mostrando el mensaje
🧪 PRUEBA REAL QUE DEBES HACER
Simula un error:
Ejemplo:
RFID duplicado
SP devuelve:
Plain text
codigo = 3
mensaje = "Error: El RFID ya existe. Verifique."
👉 Debe pasar esto:
🔊 sonido error
❌ FormAlertaError
❌ NO limpiar todo
🔄 permitir reintento
🔥 5. INTEGRACIÓN FINAL CORRECTA
En VinculadorService
C#
if (codigo == 0)
{
    OnInsertadoOk(data);
}
else
{
    OnError(mensaje);
}
En frmLector
✔ ERROR
C#
private void Vinculador_OnError(string msg)
{
    SetEstado(msg, Color.LightCoral);
    ReproducirError();

    MostrarAlerta(msg);
}
✔ OK
C#
private void Vinculador_OnInsertadoOk(Dictionary<string, object> data)
{
    ReproducirOk();

    AlertaManager.MostrarAlerta("Registrado OK", pinturaVerdeMedio, 1, 5);

    LimpiarTodo();
}
⚠️ 6. DETALLE CRÍTICO DE TU SP
Tu SP tiene esto:
SQL
INSERT INTO timb_error_web VALUES(...)
👉 Eso significa:
hay errores que NO salen directo
sino que se guardan en tabla
💡 FUTURO (recomendado): leer esa tabla también
🧠 CONCLUSIÓN
✔ Tus formularios están bien
❌ El problema NO es UI
🔥 El problema es:
👉 manejo del resultado del SP
🚀 SI QUIERES EL SIGUIENTE NIVEL
Te puedo ayudar a:
mapear TODOS los errores del SP → UI (sweet, alertify, etc.)
diferenciar tipos de alerta automáticamente
agregar logging en C#
Solo dime:
👉 “optimiza el flujo completo lector + sybase”