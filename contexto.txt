Perfecto, vamos directo a resolver esos 2 errores reales de compilación sin romper tu flujo actual 👇
❌ ERROR 1

CS0103: The name 'TabMenu_DrawItem' does not exist
🔍 Causa
Estás usando:
C#
tabMenu.DrawItem += TabMenu_DrawItem;
pero NO existe ese método en tu clase.
✅ Solución
Agrega este método en frmLector.cs (dentro de la clase):
C#
private void TabMenu_DrawItem(object sender, DrawItemEventArgs e)
{
    TabControl tab = sender as TabControl;
    if (tab == null) return;

    TabPage page = tab.TabPages[e.Index];
    Rectangle rect = e.Bounds;

    bool selected = (e.State & DrawItemState.Selected) == DrawItemState.Selected;

    Color backColor = selected ? Color.FromArgb(0, 120, 215) : Color.FromArgb(235, 235, 235);
    Color textColor = selected ? Color.White : Color.Black;

    using (SolidBrush brush = new SolidBrush(backColor))
    {
        e.Graphics.FillRectangle(brush, rect);
    }

    TextRenderer.DrawText(
        e.Graphics,
        page.Text,
        new Font("Segoe UI", 10F, FontStyle.Bold),
        rect,
        textColor,
        TextFormatFlags.HorizontalCenter | TextFormatFlags.VerticalCenter
    );
}
❌ ERROR 2

CS0029: Cannot implicitly convert type 'void' to 'int'
🔍 Causa
Seguramente tienes algo como:
C#
int total = ActualizarTotalCount(); // ❌
pero tu método:
C#
private void ActualizarTotalCount()
👉 retorna void, no int.
✅ Soluciones posibles
✔ Opción A (RECOMENDADO)
No asignes el resultado:
C#
ActualizarTotalCount();
✔ Opción B (si quieres devolver valor)
Modifica el método:
C#
private int ActualizarTotalCount()
{
    int total = DataGridView1.AllowUserToAddRows
        ? DataGridView1.Rows.Count - 1
        : DataGridView1.Rows.Count;

    if (total < 0)
        total = 0;

    lblTotalCount.Text = total.ToString();

    if (total >= MAX_CACHE_SIZE)
    {
        LimpiarTodoPorLimite();
    }

    return total;
}
⚠️ IMPORTANTE (TU LÓGICA ACTUAL)
Tu sistema YA tiene:
C#
LimpiarTodo()
LimpiarSoloRfid()
👉 NO debes duplicar lógica.
✅ INTEGRACIÓN CORRECTA CON TU SISTEMA
En lugar de crear otra función nueva, usa tu propia lógica:
🔁 Ajusta ActualizarTotalCount
C#
private void ActualizarTotalCount()
{
    int total = DataGridView1.AllowUserToAddRows
        ? DataGridView1.Rows.Count - 1
        : DataGridView1.Rows.Count;

    if (total < 0)
        total = 0;

    lblTotalCount.Text = total.ToString();

    // 🔥 reutiliza tu método existente
    if (total >= MAX_CACHE_SIZE)
    {
        LimpiarTodo();
        SetEstado("Se alcanzaron 500 registros. Limpieza automática.", Color.Khaki);
    }
}
💡 Mejora PRO (opcional)
Para que el usuario se entere:
C#
if (total >= MAX_CACHE_SIZE)
{
    AlertaManager.MostrarAlerta(
        "Se alcanzaron 500 registros. Limpieza automática.",
        pinturaGrisOscuro,
        2,
        5
    );

    LimpiarTodo();
}
⚠️ WARNINGS (no críticos)
1

FormAlerta.conteo nunca usado
👉 puedes eliminar ese campo o ignorarlo
2

_hojaMarcacionActual no usado
👉 Si no lo usas:
elimínalo
o
úsalo en validación
🚀 RESUMEN
Corrige esto y compila sin errores:
✔ Agregar método
TabMenu_DrawItem
✔ Corregir uso
no hacer int x = ActualizarTotalCount();
✔ Reusar tu lógica
usar LimpiarTodo() (no duplicar)
🔥 SI QUIERES EL SIGUIENTE NIVEL
Te puedo dejar:
🔊 sonido real scanner OK / ERROR
🟢 highlight de fila insertada
⚡ animación tipo sistema industrial (parpadeo verde/rojo)
📊 contador RFID en tiempo real con color dinámico
Solo dime:
👉 "modo planta industrial real"