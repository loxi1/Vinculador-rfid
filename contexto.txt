Sí, aquí el ajuste era mío: te propuse una versión simplificada, pero en tu repo el sistema de alertas no es uno solo. Según frmLecto.txt, para conexión exitosa se usa AlertaOk("Conexion", ...), no un toast genérico, y además conviven alertas modales de error, alertas toast y alertas OK. �
GitHub
Los archivos que sí debes conservar y portar tal como están conceptualmente son estos, con sus mismos nombres:
AlertaManager
FormAlerta
FormAlertaError
FormAlertaOk
FormAlertaMsnError
FormAviso
FormConfirmacion
Y en frmLector.cs no debes inventar nombres nuevos de controles para esas vistas; hay que adaptar la lógica a los nombres reales que ya copiaste del UI.
Qué cambios sí te recomiendo, pero usando tus archivos reales
1. No reemplazar todo por AlertaManager
Tu repo usa al menos tres patrones distintos:
MostrarAlerta(...) / AlertaManager.MostrarAlerta(...) para alertas flotantes.
AlertaError(...) / AlertaErrorMsn(...) con FormAlertaError para errores modales.
AlertaOk(...) con FormAlertaOk para éxito modal de conexión.
Esto se ve también en el flujo de conexión del lector, donde se usa AlertaOk("Conexion", pinturaVerdeMarMedio, 30, "Exitosa al reader."). �
GitHub
2. En frmLector.cs, agrega wrappers con esos nombres lógicos
No cambies tus controles; agrega métodos helper que llamen a tus formularios reales.
Deberías tener algo así:
C#
private void AlertaErrorMsn(string mensaje, Color color)
{
    toolStripStatusLbl.Text = mensaje;
    using (var alerta = new FormAlertaError("Error", mensaje, color))
    {
        alerta.ShowDialog();
    }
}

private void MostrarAlerta(string mensaje, Action callback = null)
{
    toolStripStatusLbl.Text = mensaje;

    if (FormAlertaError.alertaAbierta)
        return;

    using (var alerta = new FormAlertaError("Error", mensaje, pinturaRojoLadrillo, callback))
    {
        alerta.ShowDialog();
    }
}

private void Alerta(string mensaje, Color color, int tipo, int tiempo = 10)
{
    using (var alerta = new FormAlerta(mensaje, color, tipo, tiempo))
    {
        alerta.ShowDialog();
    }
}

private void AlertaOk(string titulo, Color color, int tiempo, string descripcion)
{
    using (var alerta = new FormAlertaOk(titulo, color, tiempo, descripcion))
    {
        alerta.ShowDialog();
    }
}
Eso respeta exactamente la idea del VB que pegaste.
3. Mapeo correcto de uso en frmLector.cs
Usa este criterio:
error de negocio de SaveRFID → MostrarAlerta(...) o AlertaErrorMsn(...)
registro correcto → AlertaManager.MostrarAlerta("Registrado Ok", ...)
conexión correcta del reader → AlertaOk("Conexion", ...)
mensajes de barra → toolStripStatusLbl
Dónde te estaba dando mal los nombres
Tienes razón en esto: yo te di nombres genéricos como dgvRegistros o variantes de labels que no necesariamente existen en tu diseñador actual. Con tu repo, por ahora hay que trabajar solo con los nombres que ya mencionaste y vienes usando en frmLector.cs, por ejemplo:
CodBarras
dgvTagList
cantidadRFID
nroOP
nroHM
lblEstadoConexion
toolStripStatusLbl
No conviene inventar más nombres hasta revisar el .Designer.cs.
Qué archivo tocaría ahora
Yo haría esto en este orden:
No tocar aún FormAlerta.vb, FormAlertaError.vb, FormAlertaOk.vb, FormAviso.vb, FormAlertaMsnError.vb, FormConfirmacion.vb si ya existen y están bien visualmente.
Portar primero AlertaManager.vb a AlertaManager.cs.
En frmLector.cs, agregar los wrappers AlertaErrorMsn, MostrarAlerta, Alerta, AlertaOk.
Reemplazar en Vinculador_OnError y Vinculador_OnInsertadoOk las llamadas simples por esas alertas reales.
Ajuste directo para tu caso actual
Error al guardar
En Vinculador_OnError:
C#
private void Vinculador_OnError(string msg)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<string>(Vinculador_OnError), msg);
        return;
    }

    toolStripStatusLbl.Text = msg;
    ReproducirError();
    CodBarras.Clear();
    CodBarras.Focus();

    MostrarAlerta(msg);
}
Éxito al guardar
En Vinculador_OnInsertadoOk:
C#
private void Vinculador_OnInsertadoOk(Dictionary<string, object> data)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<Dictionary<string, object>>(Vinculador_OnInsertadoOk), data);
        return;
    }

    if (data.ContainsKey("op"))
        nroOP.Text = Convert.ToString(data["op"]);

    if (data.ContainsKey("hoja_marcacion"))
        nroHM.Text = Convert.ToString(data["hoja_marcacion"]);

    ReproducirOk();
    AlertaManager.MostrarAlerta("Registrado Ok", pinturaVerdeMedio, 1, 5);

    LimpiarTodo();
    toolStripStatusLbl.Text = "OK";
}
Conexión correcta del reader
Cuando conecte bien:
C#
AlertaOk("Conexion", pinturaVerdeMarMedio, 30, "Exitosa al reader.");
Eso sí coincide con tu flujo VB. �
GitHub
Conclusión
Sí, corrijo eso: no debes unificar todo en una sola alerta, ni cambiar nombres de inputs que ya existen en tu UI. En tu repo hay varios tipos de mensaje y hay que respetarlos.
El siguiente paso correcto es que me pegues frmLector.Designer.cs o al menos la parte de controles del formulario, y con eso te devuelvo los métodos de alerta ajustados a tus nombres reales, sin inventar ninguno.