Sí, ahora sí ya se ve claro el mapa real de tus formularios de alerta y sus nombres exactos. Con lo que pegaste, los controles correctos son estos:
FormAlerta: MSNAlerta, FranjaTop, icon_info, icon_error, icon_ok, tiempo
FormAlertaError: icon_error, TituloAviso, DescripcionAviso, BtnAceptar, FranjaAbajo
FormAlertaMsnError: icon_error, TituloAviso, DescripcionAviso, BtnAceptar, FranjaAbajo
FormAlertaOk: icon_ok, TituloAviso, DescripcionAviso, FranjaAbajo, Tiempo
FormConfirmacion: pictureBox1, TituloAviso, DescripcionAviso, FranjaAbajo, btnAceptar, btnCancelar
Y sí: no debo inventarte otros nombres.
Qué archivos sí involucran alertas
Por lo que muestras, los archivos que debes portar o ajustar en C# son:
AlertaManager.cs
FormAlerta.cs
FormAlertaError.cs
FormAlertaMsnError.cs
FormAlertaOk.cs
FormConfirmacion.cs
FormAviso puede quedar para después, porque no parece crítico para el flujo principal de lector.
Cambios concretos que te recomiendo
1) AlertaManager.cs
Ese sí debes tenerlo en C# casi igual al VB. Ese archivo sí es necesario para FormAlerta.
2) FormAlerta.cs
Debe respetar:
MSNAlerta.Text
MSNAlerta.ForeColor
FranjaTop.BackColor
visibilidad de icon_ok, icon_info, icon_error
animación desde abajo
cierre con fade
llamar AlertaManager.RemoverAlerta(this)
3) FormAlertaError.cs
Debe respetar:
public static bool alertaAbierta
TituloAviso.Text
DescripcionAviso.Text
FranjaAbajo.BackColor
botón BtnAceptar
callback opcional
ShowDialog()
Ese es el que usarías para errores fuertes de negocio, por ejemplo cuando SaveRFID devuelve mensaje de Sybase.
4) FormAlertaMsnError.cs
Es muy parecido a FormAlertaError, pero por nombre parece orientado a solo mostrar mensaje.
Puedes reutilizar la misma lógica, cambiando solo el nombre de la clase.
5) FormAlertaOk.cs
Debe respetar:
TituloAviso
DescripcionAviso
FranjaAbajo
Tiempo.Tick
ShowDialog()
Ese te sirve muy bien para el caso de conexión exitosa al scanner.
6) FormConfirmacion.cs
Debe devolver DialogResult.OK o DialogResult.Cancel, igual que en VB.
Lo importante para frmLector.cs
Con estas clases reales, en frmLector.cs solo necesitas wrappers con los nombres funcionales del VB, no nombres de controles nuevos.
Métodos helper
Pon estos en frmLector.cs:
C#
private void AlertaErrorMsn(string mensaje, Color color)
{
    toolStripStatusLbl.Text = mensaje;

    using (var alertaError = new FormAlertaError("Error", mensaje, color))
    {
        alertaError.ShowDialog();
    }
}

private void MostrarAlerta(string mensaje, Action callback = null)
{
    toolStripStatusLbl.Text = mensaje;

    if (FormAlertaError.alertaAbierta)
        return;

    using (var alertaError = new FormAlertaError("Error", mensaje, pinturaRojoLadrillo, callback))
    {
        alertaError.ShowDialog();
    }
}

private void Alerta(string mensaje, Color color, int tipo, int tiempo = 10)
{
    using (var alerta = new FormAlerta(mensaje, color, tipo, tiempo))
    {
        alerta.ShowDialog();
    }
}

private void AlertaOk(string titulo, Color color, int tiempo, string descripcion)
{
    using (var alerta = new FormAlertaOk(titulo, color, tiempo, descripcion))
    {
        alerta.ShowDialog();
    }
}
Cómo usar cada una
Error de SaveRFID
En Vinculador_OnError:
C#
private void Vinculador_OnError(string msg)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<string>(Vinculador_OnError), msg);
        return;
    }

    SetEstado(msg, Color.LightCoral);
    ReproducirError();

    CodBarras.Clear();
    CodBarras.Focus();

    MostrarAlerta(msg);
}
Registro correcto
En Vinculador_OnInsertadoOk:
C#
private void Vinculador_OnInsertadoOk(Dictionary<string, object> data)
{
    if (InvokeRequired)
    {
        BeginInvoke(new Action<Dictionary<string, object>>(Vinculador_OnInsertadoOk), data);
        return;
    }

    if (data.ContainsKey("op"))
        nroOP.Text = Convert.ToString(data["op"]);

    if (data.ContainsKey("hoja_marcacion"))
        nroHM.Text = Convert.ToString(data["hoja_marcacion"]);

    ReproducirOk();
    AlertaManager.MostrarAlerta("Registrado Ok", pinturaVerdeMedio, 1, 5);

    LimpiarTodo();
    SetEstado("OK", Color.LightGreen);
}
Conexión exitosa al scanner
Cuando se conecte bien:
C#
AlertaOk("Conexion", pinturaVerdeMarMedio, 30, "Exitosa al reader.");
Eso sí coincide con tu flujo original.
Lo que detecté en tu material
Hay un detalle importante: en lo que pegaste, FormAviso.Designer.vb parece estar duplicado con el diseñador de FormAlertaOk, porque declara:
C#
partial class FormAlertaOk
Eso se ve mal nombrado o copiado raro. Ahí revisaría si:
FormAviso realmente existe como clase aparte
o pegaste dos veces el diseñador de FormAlertaOk
Ese punto sí lo corregiría antes de compilar.
Qué sigue
Lo más práctico ahora es que yo te deje el código C# de estas tres clases primero, usando exactamente tus controles del diseñador:
FormAlerta.cs
FormAlertaError.cs
FormAlertaOk.cs
Esas tres ya te resuelven casi todo el flujo del lector.