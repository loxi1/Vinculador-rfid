Sí, vi el avance y ya quedó mejor encaminado. En el repo actual hay tres cosas importantes:
El RFID ya entra al grid, pero OnBarcodeEventLector() todavía manda RFID a AgregarRfid(valor) y no a SetRfid(valor). �
GitHub +1
El código de barras leído por el scanner no actualiza _ultimoCodigoBarra, porque SetCodigoBarras() solo llena CodBarras.Text y el valor interno solo se asigna en CodBarras_KeyDown al presionar Enter. Si el scanner llena el textbox por evento y no por teclado físico, no se vinculará automáticamente. �
GitHub +1
Tus eventos Enter y Leave de CodBarras sí existen, pero ahora solo limpian el placeholder "Codigo de Barras..."; no afectan la vinculación. �
GitHub +1
Qué te recomiendo cambiar ahora
1) Cambiar el flujo del RFID
En OnBarcodeEventLector() cambia esto:
C#
if (PareceRfid(valor))
{
    AgregarRfid(valor);
}
else
{
    SetCodigoBarras(valor);
}
por esto:
C#
if (PareceRfid(valor))
{
    SetRfid(valor);
}
else
{
    SetCodigoBarras(valor);
}
Porque SetRfid() sí:
guarda _ultimoRfid
agrega al grid
llama IntentarVincular() �
GitHub
2) Corregir SetCodigoBarras()
Ahora mismo tu método solo pinta el textbox. Haz que también actualice _ultimoCodigoBarra y dispare la vinculación:
C#
private void SetCodigoBarras(string codigo)
{
    BeginInvoke(new Action(() =>
    {
        _ultimoCodigoBarra = (codigo ?? "").Trim();
        CodBarras.Text = _ultimoCodigoBarra;
        toolStripStatusLbl.Text = "Código de barras leído";

        IntentarVincular();
    }));
}
Con eso ya no dependerás solo de CodBarras_KeyDown. Porque hoy esa asignación solo ocurre con Enter manual. �
GitHub +1
3) Simplificar AgregarRfid()
Si vas a usar SetRfid(), puedes dejar AgregarRfid() solo como helper visual o incluso quitarlo después. En el estado actual, ese método quedó “viejo”, porque valida _rfidLeidos y pone estado, pero no es el camino completo de vinculación que sí hace SetRfid(). �
GitHub +1
Cómo debe quedar el flujo final
RFID leído por Zebra
OnBarcodeEventLector()
PareceRfid(valor) == true
SetRfid(valor)
guarda _ultimoRfid
inserta en dgvTagList
IntentarVincular()
Código de barras leído por Zebra
OnBarcodeEventLector()
PareceRfid(valor) == false
SetCodigoBarras(valor)
guarda _ultimoCodigoBarra
IntentarVincular()
Resultado
Si ya existe uno de los dos, al llegar el segundo se encola la vinculación.
Sobre Enter, Leave y KeyDown
Vas bien dejándolos, pero yo los usaría así:
Enter y Leave: solo UI del placeholder
KeyDown: soporte para ingreso manual
no confiar solo en KeyDown para el scanner, porque el scanner está entrando por OnBarcodeEventLector() en tu implementación actual. �
GitHub +2
Cambio mínimo que te desbloquea
Haz solo estas dos ediciones y pruebas:
C#
// OnBarcodeEventLector
if (PareceRfid(valor))
{
    SetRfid(valor);
}
else
{
    SetCodigoBarras(valor);
}
C#
private void SetCodigoBarras(string codigo)
{
    BeginInvoke(new Action(() =>
    {
        _ultimoCodigoBarra = (codigo ?? "").Trim();
        CodBarras.Text = _ultimoCodigoBarra;
        toolStripStatusLbl.Text = "Código de barras leído";
        IntentarVincular();
    }));
}
Después de eso:
RFID debe mostrarse en dgvTagList
barcode debe quedar en CodBarras
al tener ambos, debe entrar a IntentarVincular()
El siguiente paso sería revisar VinculadorService.cs para confirmar que la cola está guardando exactamente como en tu guía .NET.